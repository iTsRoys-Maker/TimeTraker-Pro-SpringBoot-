# Base de datos PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=timetrack
PGUSER=postgres
PGPASSWORD=postgres

# Backend (Spring Boot)
JWT_SECRET=cambia-esto-por-una-cadena-aleatoria-de-al-menos-32-bytes
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost

# Frontend (Vite)
PORT=5173
BASE_PATH=/
VITE_API_URL=http://localhost:8080
