# URL del backend Spring Boot (incluye /api).
# En desarrollo no es necesaria si usas el proxy de Vite (configurado en vite.config.ts):
# todas las llamadas a /api/* se reenviarán automáticamente a http://localhost:8080/api/*.
VITE_API_URL=http://localhost:8080/api
